import { FixNewLinesPipe } from './fix-new-lines.pipe';

describe('FixNewLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new FixNewLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
