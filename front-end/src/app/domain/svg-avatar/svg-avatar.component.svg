<svg viewBox="0 0 100 100" height="100%">
  <circle cx="50" cy="50" r="45" [attr.fill]="backColor" />
  <text text-anchor="middle" dominant-baseline="central" x="50" y="50" [attr.fill]="textColor" [attr.font-size]="(text ? 20 : 40)"
   font-weight="bold">{{text || defaultText}}</text>
</svg>
